import { FacebookShareDirective } from './facebook-share.directive';

describe('FacebookShareDirective', () => {
  it('should create an instance', () => {
    const directive = new FacebookShareDirective();
    expect(directive).toBeTruthy();
  });
});
