import { TwitterShareDirective } from './twitter-share.directive';

describe('TwitterShareDirective', () => {
  it('should create an instance', () => {
    const directive = new TwitterShareDirective();
    expect(directive).toBeTruthy();
  });
});
